# The Cheezeyverse commissioner.
#
#     pip install -r requirements.txt
#
# Windows only, and that is not an accident: FBPB3 is a VB6 application with no command line
# and no API, so the whole driver is Win32 GUI automation. There is nothing here to run on a
# Mac or a Linux box.
#
# `.env` is NOT read by a library. commissioner/settings.py parses it itself (see _load_dotenv),
# which is why python-dotenv is absent despite the file being called .env.

# The commissioner panel: the Sim Week button and the offseason.
Flask==3.1.0

# Drives FBPB3. Owner-drawn VB6 controls ignore posted messages, so this sends real mouse
# input - which is also why the machine running it needs a live desktop. See docs/SERVER.md.
pywinauto==0.6.9

# Comes with pywinauto, pinned because the driver uses win32gui/win32con directly to force the
# game window to the top before every real click.
pywin32==312

# Supabase over plain PostgREST. No SDK: it is a REST API and this is eight calls.
requests==2.32.5

# Screenshots, used by the driver when a step needs to prove what was on screen.
Pillow==12.0.0
